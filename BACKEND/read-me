#necessário as bibliotecas:

sqlalchemy
sqlalchemy.orm
fastapi
uvicorn
passlib[bcrypt]
dotenv
alembic
pip install bcrypt==4.3.0
python-jose[criptograpy]
pymysql
python-multipart
'pydantic[email]'

#pra instalar é so rodar no terminal:

pip install sqlalchemy sqlalchemy.orm fastapi uvicorn passlib dotenv alembic bcrypt==4.3.0 python-jose[cryptography] pymysql python-multipart 'pydantic[email]'

#para rodar o código: uvicorn main:app --reload

#para iniciar o banco
#alembic init alembic

#iniciar migração
#alembic revision --autogenerate -m "mensagem de mudança"

#executar a mudança:
#alembic upgrade head